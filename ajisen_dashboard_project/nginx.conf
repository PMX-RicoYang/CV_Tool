server {
    # 🚀 優化 1: 開啟 http2 (多工傳輸，大幅加速靜態資源載入)
    listen 443 ssl http2;
    server_name _;

    ssl_certificate     /etc/nginx/certs/server.crt;
    ssl_certificate_key /etc/nginx/certs/server.key;

    ssl_protocols       TLSv1.2 TLSv1.3;
    ssl_ciphers         HIGH:!aNULL:!MD5;

    # 🚀 優化 2: 開啟 Gzip 壓縮 (最關鍵的加速設定)
    # Grafana 的 JS 檔案很大，壓縮後會從 MB 級別變 KB 級別
    gzip on;
    gzip_disable "msie6";
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;      # 壓縮等級 (1-9)，6 是效能與速度的最佳平衡
    gzip_buffers 16 8k;
    gzip_http_version 1.1;
    gzip_min_length 256;    # 太小的檔案不壓縮
    
    # 設定要壓縮的檔案類型 (包含 JS, CSS, JSON, SVG)
    gzip_types
        text/plain
        text/css
        application/json
        application/javascript
        text/xml
        application/xml
        application/xml+rss
        text/javascript
        application/vnd.ms-fontobject
        application/x-font-ttf
        font/opentype
        image/svg+xml
        image/x-icon;

    # ---- 靜態 index.html / 圖片等 ----
    root /usr/share/nginx/html;
    index index.html;

    # 🚀 優化 3: 針對 Grafana 靜態資源開啟瀏覽器快取
    # 讓瀏覽器快取這些圖片和 JS，第二次載入會是秒開
    location ~ ^/grafana/public/ {
        # 必須指向 Grafana
        proxy_pass http://grafana:3000;
        
        # 移除原本可能禁止快取的 header
        proxy_hide_header Cache-Control;
        proxy_hide_header Pragma;
        
        # 強制瀏覽器快取 1 年 (31536000秒)
        add_header Cache-Control "public, max-age=31536000, immutable";

        # 保持原本的 Header 設定
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Script-Name /grafana;
    }

    # ---- API 反向代理 ----
    location /quality/ {
        proxy_pass http://api:5001;
        proxy_http_version 1.1;

        proxy_set_header Host              $host;
        proxy_set_header X-Real-IP         $remote_addr;
        proxy_set_header X-Forwarded-For   $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }

    # ---- Grafana 反向代理設定 ----
    location /grafana/ {
        # 轉發給內部網路中的 Grafana 服務
        proxy_pass http://grafana:3000;
        proxy_http_version 1.1;

        # WebSocket 設定
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
        proxy_read_timeout 86400;

        # 必要的 Header
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto https;
        proxy_set_header X-Script-Name /grafana;
    }
    
    # 其它路徑 → 給 index.html
    location / {
        try_files $uri /index.html;
    }
}

# HTTP 轉 HTTPS
server {
    listen 80;
    server_name _;
    return 301 https://$host$request_uri;
}